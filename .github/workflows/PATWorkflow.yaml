name: Sequential Jobs with Auto-Approve via PAT

on:
  workflow_dispatch:
env:
 KEY_VALUE: ${{ vars.VAR1}}
 SECRET_VALUE: ${{ secrets.SECRET1 }}

 
jobs:
  job1:
    environment: test   # 🔒 requires manual approval
    runs-on: ubuntu-latest
    steps:
      - name: Job1 - Manual approval step
        run: |
          echo "Job1 running after manual approval"
          
      - name: Print global env variable
        run: |
          echo "KEY_VALUE in job1= $KEY_VALUE"
          echo "SECRET value in job1= $SECRET_VALUE"
  job2:
    runs-on: ubuntu-latest
    steps:
     - name: Job1 - Manual approval step
       run: |
          echo "Job1 running after manual approval"
          
     - name: Print global env variable
       run: |
          echo "KEY_VALUE in job 2 = $KEY_VALUE"
          echo "SECRET value in job2 = $SECRET_VALUE"
    
     
    
          
